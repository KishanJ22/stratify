meta {
  name: Get a JWT token
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/token
  body: none
  auth: inherit
}

script:post-response {
  const token = res.body.token;
  bru.setEnvVar("jwt", token); 
}
