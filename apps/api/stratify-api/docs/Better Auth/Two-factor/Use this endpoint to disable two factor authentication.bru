meta {
  name: Use this endpoint to disable two factor authentication.
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/two-factor/disable
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "password": ""
  }
}
