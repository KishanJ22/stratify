---
"@stratify/stratify-data-api": patch
---

- Change format of data returned in market endpoints to avoid returning unnecessary data
- Add minimumVolume parameter to percentage change query to filter out assets that have low trading volume
